class CreateWorks < ActiveRecord::Migration
  def change
    create_table :works do |t|
      t.date :date
      t.references :inside
      t.references :outside

      t.timestamps
    end
    add_index :works, :inside_id
    add_index :works, :outside_id
  end
end
