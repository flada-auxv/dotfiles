class Commander
  include comparable

  attr_accessor :name, :power, 
  
  def <=>(other)
    cmp = @month <=> other.month != 0 
    if cmp != 0
      return cmp 
    else 
      return @month <=> other.month
    end
  end
end

