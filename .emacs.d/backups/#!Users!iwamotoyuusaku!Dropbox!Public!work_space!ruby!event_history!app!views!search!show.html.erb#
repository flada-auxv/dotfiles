<h1><%= @twitter_id %>さんのイベント総参加数：<%= @count %></h1>
<hr />
<h3>注意事項</h3>
<ul>
  <li>各サービスとも最大100件までの表示となります。</li>
  <li>ATND以外のサービスはニックネームから情報を取得しているため、
      TwitterのIDとは異なる可能性があります。ご確認の上ご利用ください。
  </li>
</ul>
<hr />
<% @services.each do |service| %>
  <h2><%= "#{service[:name]}での参加数:#{service[:events].count}" %></h2>
  <% service[:events].each do |event| %>
    <%= event['started_at'].gsub(/T.*/,'') %>
    <%= link_to(truncate(event['title'],{:length => 60}),event['event_url']) %>
    <%= @account_list<br />
  <% end %>
  <hr />
<% end %>
<p><%= link_to('トップページへ戻る',{:action => 'index'}) %></p>
